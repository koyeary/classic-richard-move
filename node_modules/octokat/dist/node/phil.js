'use strict';

var fetch = require('node-fetch');
var chained = require('./chained');
var Requestor = require('./request');

var req = new Requestor(fetch);

var method = 'GET';
var path = 'https://api.github.com' + chained('', '').repos('philschatz', 'octokat.js').issues._path;
var data = null;
var isRaw = false;
var isBase64 = false;
var isBoolean = false;
var contentType = null;
var acceptHeader = null;

req.request(method, path, data, isRaw, isBase64, isBoolean, contentType, acceptHeader).then(function (_ref) {
  var value = _ref.value,
      request = _ref.request,
      response = _ref.response,
      additional = _ref.additional;

  console.log('REQUEST', request);
  console.log('RESPONSE', response);
  console.log('ADDITIONAL', additional);
  console.log('RESPONSE_HEADERS', JSON.stringify(response.headers));
});
//# sourceMappingURL=phil.js.map